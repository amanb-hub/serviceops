"use client";

import { AppShell } from "@/components/app-shell";
import { PlaceholderModule } from "@/components/card";

export default function TicketsPage() {
  return (
    <AppShell>
      <PlaceholderModule title="Tickets" />
    </AppShell>
  );
}
